import React from "react";
import { LoginPage } from "./login-page";

/* describe("<LoginPage />", () => {
  it("renders", () => {
    cy.mount(<LoginPage />);
    cy.get(".auth").should("exist");
  });
}); */
